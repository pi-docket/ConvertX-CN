# ConvertX-CN 環境變數設定範例
# 複製此檔案為 .env 並修改設定
# cp .env.example .env

# =============================================================================
# JWT 認證設定（API Server 必須）
# =============================================================================

# JWT 密鑰（必須設定！用於 API Server 的認證）
# 建議使用至少 32 字元的隨機字串
# 產生方式：openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-me-in-production

# =============================================================================
# Web UI 設定
# =============================================================================

# Web UI 監聽端口
PORT=3000

# 允許的檔案大小上限 (bytes)
# 預設 500MB
HTTP_ALLOWED_FILE_SIZE=524288000

# 資料目錄
DATA_DIR=./data

# =============================================================================
# API Server 設定（選用）
# =============================================================================

# API Server 監聽端口
RAS_API_PORT=7890

# ConvertX Web UI 後端地址（Docker 內部網路）
CONVERTX_BACKEND_URL=http://convertx:3000

# 上傳目錄
UPLOAD_DIR=./data/uploads

# 輸出目錄
OUTPUT_DIR=./data/output

# 最大檔案大小 (bytes)
MAX_FILE_SIZE=524288000

# =============================================================================
# 進階設定（選用）
# =============================================================================

# 輸出清理設定（小時）
# 自動清理超過指定時間的輸出檔案
# OUTPUT_RETENTION_HOURS=24

# 日誌等級
# LOG_LEVEL=info

# =============================================================================
# 翻譯功能設定（選用）
# =============================================================================

# OpenAI API 密鑰（用於 PDF 翻譯功能）
# OPENAI_API_KEY=sk-your-api-key

# OpenAI API Base URL（可選，用於自訂端點）
# OPENAI_BASE_URL=https://api.openai.com/v1

# =============================================================================
# VLM 推理設定（選用）
# =============================================================================

# VLM 推理伺服器地址
# VLM_API_BASE=http://localhost:8080

# 使用的 VLM 模型
# VLM_MODEL=gemma3
